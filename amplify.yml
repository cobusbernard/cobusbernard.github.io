version: 0.1

frontend:
   phases:
      build:
          commands:
            - echo "Building the Hugo site..."
            - hugo
   artifacts:
      baseDirectory: public
      files:
         - '**/*'
cache:
  paths: []